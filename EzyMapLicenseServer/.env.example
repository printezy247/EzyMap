# Copy this file to .env and change these before deploying publicly.
PORT=3000

# Shared secret the admin CLI must send when managing subscriptions remotely.
# (Not required for local CLI use - only matters if you expose admin routes.)
ADMIN_TOKEN=change_this_to_something_random
